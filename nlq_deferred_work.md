# Deferred work — nlq

1. 2026-04-14 | migrated from H2 format | n/a | 8 harness failures on single-metric value/unit/confidence, filed during multi-metric 422 fix session. Baseline and post-edit harness runs returned identical 97/8. Failures: PL_001 EBITDA 2025 (value/unit None, conf 0.5); PL_002 2025 P&L (revenue/cogs/gross_profit/ebitda/net_income all None); PL_003 gross margin Q3 2025 (value/unit None); PL_004 net income margin (value/unit None, conf 0.5); PERIOD_001 2025 revenue (value None); PERIOD_002 2024 revenue (value None); ALIAS_003 gross margin (unit None); CLARIFY_002 "show me the margin" (conf 0.3). None involve multi-metric decomposition — out of scope for 422 fix. Response returns success=True with data_source=None and null values instead of surfacing a real error. Likely another reclassification or dcl_v2 path with missing data. | severity: degraded | blocking: 8 single-metric harness queries return false success with a null payload; masks an underlying reclassification or dcl_v2 data gap

2. 2026-04-15 | migrated from H2 format | src/components/DebugTracePanel.tsx:71-75 + routes.py:5079 | DebugTracePanel hint regression (cosmetic): the panel gates the ANTHROPIC_API_KEY hint on `error_type === 'CONFIG_ERROR'`. After the HTTPException re-raise fix in routes.py:5079, App.tsx now sets `error_type = 'HTTP_ERROR'` for all non-200 responses, so the hint no longer fires for the missing-API-key case. The underlying error text ("ANTHROPIC_API_KEY not configured") is still visible in the banner. The hint itself references a CLAUDE.md-banned platform. Replacement: match on error text directly (`debugInfo?.error?.includes('ANTHROPIC_API_KEY')`) and drop the banned copy. A13 scope. (date unknown, migration default) | severity: cosmetic | blocking: n/a

3. 2026-04-14 | migrated from H2 format | n/a | Empty-set UX envelope: the distinction between `value=0` (Farm emitted a zero triple) and ∅ (Farm emitted no triples) is now preserved in code, but the user-facing envelope is shared — both flow through `WidgetData { value, formatted_value, trend, sparkline_data }` where null-value widgets surface as "No data for X". Real zero should render "0" or "$0" with trend; empty set should render an honest "not generated for this entity" indicator. Proposed shape: add `status: "ok" | "empty" | "error"` and `reason` fields to widget responses, wire through the React renderer. Needs UX discussion before code. | severity: degraded | blocking: widget envelope cannot distinguish real zero from missing data; operators see "No data" for both

4. 2026-04-15 | migrated from H2 format | metric_concept_map.yaml:426 + dcl_semantic_client_v2.py:get_derived_metric | burn_multiple division-by-zero: `burn_multiple` is defined as `cash_flow.net_burn / revenue.recurring`. If Farm emits `revenue.recurring = 0` (or the concept is absent), the derived formula divides by zero. The current derived-metric path in `get_derived_metric` does not guard. Needs a decision: return null with `reason="undefined"`, return infinity, or fall back to a warning flag. Separate from the ARR fix — this is a derived-metric robustness question, not a concept-mapping one. (date unknown, migration default) | severity: degraded | blocking: derived-metric robustness; any query resolving to burn_multiple on a zero-recurring-revenue entity will divide by zero

5. 2026-04-15 | migrated from H2 format | n/a | ME-boundary cleanup in NLQ: NLQ must remain entity-agnostic. Any vertical-specific or multi-entity logic belongs in the M&A service or in Farm generators, not in NLQ metric maps, test cases, or resolver code. Ongoing vigilance: any future code or test that assumes an entity's vertical should be flagged as a RACI violation (ME concerns live in the M&A service per v7.0). The NLQ pre-commit hook already enforces this for SE-only patterns. (date unknown, migration default) | severity: cosmetic | blocking: n/a (ongoing rule, not a pending fix)

6. 2026-04-15 | path 1 landed in multi-year-revenue fix | tests/harness/test_cases.yaml | pre-existing entity contamination in harness fixture: the file has 31 entities pinned to legacy ME-era seed names plus matching comment references. **Path 1 (hook-scope fix) landed 2026-04-15** — `.git/hooks/pre-commit` now scans only ADDED lines (`git diff --cached --unified=0 | grep -E '^\+[^+]'`), so edits that don't introduce new forbidden patterns are allowed through. New cases must use current SE entity_ids (e.g. HelixEdge-0X4F). **Path 2 (full fixture rewrite) still pending**: replace legacy names with SE entity_ids and recalibrate every value range against current Farm output via DCL queries — also resolves the 8 pre-existing harness failures in item 1 (PL_001-004, PERIOD_001-002, ALIAS_003, CLARIFY_002 silently return null under legacy names, addressing D6). | severity: degraded | blocking: D6 remediation of 8 pre-existing harness failures still gated until path 2 is chosen

7. 2026-04-15 | migrated from H2 format | n/a | Farm arr.ending is identical across entities: direct DCL query confirms HelixEdge-0X4F and SysLabs-ANYC both return `arr.ending` series `89.61, 96.05, 102.95, 110.35, 116.91...` for 2024 quarters — byte-identical. NLQ resolves to $152.3M regardless of entity, which is correct given Farm's output. This is a Farm data-generation concern, not an NLQ concern. Entity-agnostic ARR resolution works; entity-specific values are Farm's responsibility per RACI. (date unknown, migration default) | severity: degraded | blocking: cross-entity divergence in Farm's arr.ending generation (Farm-owned per RACI; NLQ cannot fix)

8. 2026-04-15 | filed during multi-year revenue fix | routes.py:_try_multi_metric_query period_re | Unsupported period literal forms in the multi-period classifier: the classifier only accepts `^20\d\d$` and `^20\d\d-Q[1-4]$`. Forms that fall through to the LLM path at step 14 (or misclassify entirely): `FY2024` (fiscal-year prefix), `2024 Q2` (space-separated quarter), `Q2 2024` (quarter-first), `2024-2026` (year range), `last 3 years` (relative window). Intentional scope for the multi-year fix — the narrow regex prevents false positives on metric tokens that contain digits. Replacement: extend the classifier with explicit patterns per form, OR introduce a dedicated period-parser with an enum of supported shapes. Do not widen the regex ad-hoc; each form needs its own normalization to a canonical `time_range`. | severity: degraded | blocking: users asking for multi-period series in any of these forms get LLM-path fallback, not the deterministic DCL fan-out

9. 2026-04-15 | filed during multi-year revenue fix | src/nlq/knowledge/synonyms.py:~888 normalize_metric | `normalize_metric` legacy fallback returns input lowercased on miss — silent A1 violation. Function is "try match, else return input.lower()". Any caller treating the return as a canonical metric name will accept arbitrary garbage (year literals, free text, typos) as a valid metric, which is what originally let `['revenue_for_2024', '2025', '2026']` pass the decomposer. Fix: replace legacy fallback with `raise UnknownMetricError(token)`, audit all callers to expect the raise, update tests. Routed around in multi-year-revenue fix via `get_all_metrics()` set intersection — the root cause is still live for every other caller. | severity: degraded | blocking: every caller of `normalize_metric` silently accepts unknown tokens as metrics; additional decomposer-style bugs latent across the codebase

10. 2026-04-15 | this session | .git/hooks/pre-commit | nlq has no scripts/precommit.sh source file — installed hook exists only in .git/hooks/pre-commit. Whitelist entry for nlq_deferred_work.md was added directly to the installed hook this session and is session-local (will not survive fresh clone or hook reinstall). Hook source must be moved to scripts/precommit.sh (matching dcl pattern) and installation automated. Severity: degraded. Blocking: any fresh clone of nlq will re-trigger hook block on commits touching nlq_deferred_work.md until manually re-whitelisted.

11. 2026-04-09 | https://claude.ai/chat/5f7c4a6a-d5f6-4137-be0b-78b110bef2a8 | tests/ | 20 pre-existing test failures flagged and skipped by agent during provenance/dropdowns work. D6 violation. Severity: degraded. Blocking: 100% pass invariant.

12. 2026-04-09 | https://claude.ai/chat/5f7c4a6a-d5f6-4137-be0b-78b110bef2a8 | DCL upstream metadata.mode="Farm" (v1 path) | v1 client path still hardcodes mode="Farm"; v2 path was fixed to derive from _live_triple sentinel. Decide: defer or kill v1 path. Severity: degraded. Blocking: provenance correctness on any surface still using v1 client.

13. 2026-04-10 | https://claude.ai/chat/645d99a8-bd92-4e24-8140-6a8fee49d5bc | tests/e2e/operator_e2e.spec.ts:195 | test asserts a request shape (no entity_id) the UI cannot construct. Likely test-rot bug from entity_id enforcement; needs delete-or-rewrite, not product fallback. Plus 7 other Playwright failures triaged behind it. Severity: degraded. Blocking: Playwright accountability gate.

14. 2026-04-17 | plan certain-queries-load-a-dynamic-lamport | farm/src/main.py:264-280 | Three manifest-intake aliases (`POST /api/manifest-intake` L268, `POST /api/manifest/execute` L274, `POST /api/ingest` L280) invoke `_execute_single_manifest(manifest)` bare — no `skip_legacy_push=True`, no `execute_and_push` wrapper. These run the legacy pipe-ingest push path (`POST {dcl_url}/api/dcl/ingest`) which DCL deprecated and now returns HTTP 410. Any AAM runner or external caller hitting these aliases will look successful at the Farm layer (generator + persist OK) but silently fail to land any triples in DCL. Same class of drift as `/api/farm/self-test` (fixed this session: L304 now uses `execute_and_push` with an explicit `entity_id` Query param). Fix: port all three aliases to `execute_and_push` and require `entity_id` on the manifest target, or delete them if no real caller exists. Discovery prompted by self-test 410 during bookings verification. | severity: degraded | blocking: any caller of `/api/manifest-intake`, `/api/manifest/execute`, or `/api/ingest` completes with status=success but zero triples in DCL — a silent-fallback class failure (A1)

15. 2026-04-17 | plan certain-queries-load-a-dynamic-lamport | src/nlq/core/visualization_intent.py:397 | CRO dashboard does NOT render bookings despite Farm emitting `sales.bookings.total.amount` and NLQ having `bookings → sales.bookings.total` wired in metric_concept_map.yaml:44. Root cause: `PERSONA_METRICS["CRO"]` tuple at visualization_intent.py:397 is `["pipeline", "win_rate_pct", "quota_attainment_pct", "sales_cycle_days", "arr", "revenue"]` — `bookings` absent. Dashboard generator caps kpi_metrics at 4 (dashboard_generator.py:609), so even adding bookings at tail position won't surface unless inserted in first 4 slots. Fix: insert `bookings` after `pipeline` in the CRO tuple → `["pipeline", "bookings", "win_rate_pct", "quota_attainment_pct", "sales_cycle_days", "arr", "revenue"]`. Not fixed this session — scope was domain audit + self-test verification, not persona tuple edits. Verified via live call to `POST /api/v1/query {"query":"show me the CRO dashboard","persona":"CRO","entity_id":"Bookings-TEST3"}` against ingest run that landed 31733 triples including bookings at 12 quarterly rows (2025-Q3 = 45.69). | severity: degraded | blocking: B17 frontend gate on CRO dashboard bookings verification — cannot claim feature done until tile renders

16. 2026-04-26 | plan dashboard-has-weird-piped-raccoon | src/components/generated-dashboard/WidgetRenderer.tsx:190 + src/hooks/useDashboardLayout.ts:167 | Two pre-existing TypeScript errors on `dev` HEAD: both compare against literal `"sales_funnel"` which is not a member of the `WidgetType` union (TS2678 + TS2367). Vite `npm run build` passes (no tsc gate) so this is silent build noise, but `tsc --noEmit` flags them. Either the union dropped `sales_funnel` and the call sites are dead code, or the union should re-add it. Verified pre-existing via `git stash && tsc --noEmit` on clean dev — identical output. Not introduced by ProvenanceBanner removal this session. | severity: cosmetic | blocking: nothing in the dev/build path; only `tsc --noEmit` and any future strict-mode typecheck

17. 2026-05-04 | plan most-ask-queries-that-twinkling-journal | src/nlq/api/routes.py:1178 | Superlative/ranking SimpleMetricResult sets `formatted_value="N/A"` literal when DCL has no ranking data (`_try_superlative_query` no-data branch). Goes through `simple_metric_to_galaxy_response` (query_helpers.py:132) into `IntentNode.formatted_value` of the IntentMapResponse — surfaces as "N/A" on the /api/v1/query/galaxy path's panel. Same class as the format_value fix this session (node_generator.py:format_value now returns None on null), but App.tsx hits /api/v1/query (NLQResponse), not /query/galaxy, so this branch is not in the user's reported surface today. Replace with `formatted_value=None` and let FE render placeholder when galaxy endpoint regains a UI consumer. | severity: cosmetic | blocking: galaxy-endpoint panel UX consistency (not user-facing today)

18. 2026-05-04 | plan most-ask-queries-that-twinkling-journal | tests/test_nlq_ground_truth.py::TestAskCosts | test_cogs and test_opex fail against live NLQ with values ~16x ground truth (e.g., Q1 OpEx expected 72.08, got 1188.83). Pre-existing — confirmed by `git stash` + rerun on clean dev. Aggregation/period mismatch in single-metric path (`_try_simple_metric_query` → `simple_metric_to_nlq_response`), not touched by this session's `node_generator.py` fix. Same class as deferred item 1 (single-metric value/unit None failures) but different test suite. Likely fix: align `_build_simple_metric_result` period semantics with `routes.py:get_val` (quarterly granularity for annual periods). | severity: degraded | blocking: D6 100%-pass invariant on tests/test_nlq_ground_truth.py

19. 2026-05-16 | WS-1 Block 3 dispatch | src/nlq/api/dashboard_routes.py:32,49,88 | `_dashboard_cache: Dict[str, DashboardSchema] = {}` is dead code: no producer writes to it anywhere in the codebase. `GET /api/v1/dashboard/{id}` and `POST /api/v1/dashboard/filter` both read this empty dict and always 404. Actual dashboard storage is per-session via `set_session_dashboard`/`get_session_dashboard` in `session.py` (DashboardSessionStore), populated by `routes.py:4649` and `routes.py:4905`. Block 3 prompt assumed prebuilt schemas named `cto` and `cloud-cost-optimization` exist with 6 tiles each (kpi_uptime_pct, kpi_cloud_spend_monthly_total, etc.) — they do not. Those tile IDs are runtime-generated by `generate_dashboard_schema()` with random `dash_<8-hex>` parent IDs, from natural-language queries via `/api/v1/query`. HALTED — Block 3 requires authorization to either (a) author prebuilt schemas + bypass session-based architecture or (b) reroute `GET /dashboard/{id}` to read from session store keyed by session_id. Provenance gap at `dashboard_data_resolver.py:677` is real but the 5 fields the prompt names (source_system, source_field, pipe_id, fabric_plane, confidence_score) are DCL-side per-triple provenance — NLQ's `run_provenance` only surfaces aggregated `source_systems`. Needs cross-module contract decision before NLQ resolver code can plumb per-tile per-triple provenance. | severity: blocker | blocking: WS-1 Block 3 cannot proceed without product/architecture authorization on schema-author scope and provenance contract shape
    REDIRECTED 2026-05-16 (Ilya, WS-1 closeout) — Block 3 closed as architecture-premise correction; no schema authoring authorized. Per deck Slide 11 the named-dashboard model is runtime-generated ("user asks the question, NLQ renders as dashboard with drill-through"); the runtime path already works for dashboard rendering. No prebuilt `cto` / `cloud-cost-optimization` schemas needed. `/api/v1/dashboard/{id}`, `POST /api/v1/dashboard/filter`, and `_dashboard_cache` remain dead code today and can be deleted as a future NLQ cleanup chore — not on the WS-1 critical path. The per-triple provenance gap is reclassified as a **WS-5 (AOS-MCP Server) prerequisite**, not a tactical NLQ fix. AOS-MCP Server defines the serving contract for every consumer (FinOps agent, AOS Intelligence dashboard drill-through, simulated BI federation), so provenance must surface end-to-end. Cross-module scope: (1) **DCL** — `/api/dcl/triples/browse` and every equivalent query endpoint must return per-triple provenance in the response shape (source_system, source_field, pipe_id, fabric_plane, confidence_score). Fields already exist on each row; audit every DCL query endpoint to confirm shape parity. (2) **NLQ** — `dashboard_data_resolver.py` widget resolvers (`_resolve_kpi_data`, `_resolve_time_series_data`, etc.) must thread per-triple provenance from the resolver layer onto every widget return dict; today `_provenance` is captured at resolver scope but dropped before reaching widget data. (3) **AOS-MCP Server** — response envelope must surface provenance to consumers; envelope shape pending in WS-5. This entry stays open as the WS-5 entry point for the gap. See aam_deferred_work.md#17 for the AAM-side mirror.
    UPDATE 2026-05-31 (finops-domain-decision-d) — NLQ-side per-triple plumbing extended to **Ask**: a shared extractor (`src/nlq/services/provenance.py` PROVENANCE_FIELDS / prov_from_triple) now feeds (a) the v2 single-metric Ask path (`dcl_semantic_client_v2._propagate_provenance` reads contributing browse triples → `run_provenance.per_triple`), and (b) the v1 report/query path (`dcl_semantic_client._normalize_dcl_query_response`). dashboard_data_resolver + persona_dashboard_resolver were repointed at the shared extractor (dedup; field set was diverged). VERIFIED live: NLQ Ask "revenue for 2025" → provenance.per_triple = 4 entries (one per quarter), each {source_system, fabric_plane, confidence_score} (source_field/pipe_id null in Farm data → honestly omitted, A1). NLQ part (2) of this entry is now DONE for both Dashboards and Ask. Item (1) (DCL `/api/dcl/query` returning the 5 fields on data rows) remains OPEN — confirmed empirically this session: execute_query sets provenance=[] and QueryDataPoint lacks the 5 fields; the v2 browse path DOES carry them, which is why Ask single-metric per_triple populates. Item (3) (AOS-MCP envelope) unchanged.

20. 2026-05-20 | identity-remediation R3 whimsical-hanrahan | tests/test_nlq_v2_client.py (TestRevenueQuery, TestEntityScoped, TestDerivedMetric, TestDashboardMetrics) | The revenue / gross-margin / CFO-dashboard tests in test_nlq_v2_client.py are stale against the current FinOps demo dataset. That dataset is AR-aging (invoices, customers, vendors) and carries no `revenue` domain triples — DCL resolver logs confirm "[Resolver] No sources for concept: revenue". NLQ's query pipeline runs correctly: test_revenue_via_nlq returns success=True with a graceful "I recognize Revenue ... but I don't have data for it right now ... missing data in the current dataset for metric revenue" (value=None), which is why the assertion fails. Failing tests: test_revenue_via_nlq, test_revenue_via_dcl_triples, test_entity_scoped_via_nlq, test_gross_margin_via_nlq, test_cfo_dashboard_via_nlq. NOT R3-caused — proven: (a) test_revenue_via_nlq returns a graceful response, not a 500, so NLQ's R3 browse-batch+tenant_id threading works end-to-end; (b) test_persona_dashboards_ws5_b1.py stays 21/21 post-R3; (c) same class as pre-existing entry 18 (test_nlq_ground_truth cogs/opex stale-vs-dataset). test_revenue_via_dcl_triples was updated this session to the new browse contract (tenant_id param + tenant_id module fixture); its residual failure is the same 0-triples data gap. The 4 ERROR outcomes seen on some runs are dcl-dev wedging under burst load — see dcl_deferred_work.md#27 (counts_sql statement timeout), not a test bug. Disposition needed: delete the stale revenue/CFO tests, repoint them to a metric the FinOps dataset carries (e.g. AR aging / invoice totals), or seed revenue triples into the demo dataset — a test-hygiene + dataset call outside the identity-chain remediation scope. | severity: degraded | blocking: D6 100%-pass invariant on tests/test_nlq_v2_client.py

21. 2026-05-20 | identity-remediation R5 verification whimsical-hanrahan | DCL semantic_triples (FinOps demo ingest) + finops.yaml AR widgets | Two FinOps demo-data gaps surfaced verifying R5. (1) canonical_id / resolution_method / resolution_confidence are NULL on every FinOps triple — entity resolution was never applied to the FinOps ingest. The R5 resolution-chain plumbing is intact end-to-end (DCL browse returns the 3 columns, AAM cross-source passes them through, NLQ _PROVENANCE_FIELDS now extracts them) but it carries nulls, and _prov_from_triple omits null fields — so the restored chain shows nothing until the demo data is entity-resolved. The cross_book_vendor_overlap_table widget (whose purpose is identity-resolved vendor overlap) is consequently near-empty (1 row, null amount, null confidence). Entity resolution is a DCL/Convergence concern — the FinOps ingest needs a resolution pass. (2) AAM cross-source-query domain=invoice returns 0 triples for finops-demo-co (only AP / domain=ap_invoice has data) — the 3 AR widgets (total_ar_outstanding_kpi, ar_by_aging_bucket_chart, ar_by_source_chart) resolve to empty. The FinOps demo received only ap_invoices webhooks, no invoices (AR) webhooks. Demo-setup gap. Neither gap is R3-R6-caused or prune-caused: browse-batch filters is_active=true and the prune kept every is_active=true row. | severity: degraded | blocking: FinOps persona dashboard shows empty AR tiles + a near-empty vendor-overlap table; R5's restored resolution chain has no non-null data to carry
    UPDATE 2026-05-31 (finops-domain-decision-d) — Part (2)'s subject ("finops.yaml AR widgets": total_ar_outstanding_kpi, ar_by_aging_bucket_chart, ar_by_source_chart, cross_book_vendor_overlap_table) is now MOOT: finops.yaml was deleted this session (AAM Blueprint v3.1 decision (d) — FinOps is a domain, not a persona), so those AR widgets no longer exist. The empty-AR-tiles symptom is gone with the file. Part (1) (canonical_id/resolution_method/resolution_confidence NULL on FinOps triples — entity resolution never applied to the FinOps ingest) is a DCL/Convergence data concern, UNAFFECTED and still open.

22. 2026-05-21 | demo-pipeline-monitor | src/nlq/core/dashboard_data_resolver.py:129-146 | GET /api/nlq/monitor/metrics (new, read-only — feeds the AAM-served pipeline monitor at /aam/monitor) reports tile_errors_1h as null; the page renders it "n/a". Widget/tile resolution failures are logged to stderr and returned inline as widget_data[...]={"status":"dcl_error"|"resolution_error"} but are never counted or persisted — no table, no counter, no time-windowed store — so a "tile errors in the last 1h" figure cannot be computed from a read. Omitted per the monitor build's READ-ONLY guardrail rather than fabricated. To resolve: add a timestamped in-memory ring buffer (or counter) for widget-resolution errors and expose a 1h count. The monitor's other NLQ metrics are live from existing state — active dashboards from DashboardSessionStore, queries-1h / p95 / cache-hit from a read-only rag_learning_log query. | severity: cosmetic | blocking: nothing — the monitor renders "n/a" honestly


22. 2026-05-21 | plan map-widget-revenue-by-region | src/components/generated-dashboard/MapWidget.tsx:199-253 | MapWidget GeoJSON-countries layer crashes Leaflet on re-render/unmount races. The "Load and render GeoJSON countries" useEffect captures `map = mapRef.current`, fetches the world-borders GeoJSON async, then calls `geoJsonLayer.addTo(map)` inside the `.then` with no guard that the map is still mounted. When the widget re-renders (data update) or unmounts before the fetch resolves, the map-init effect cleanup has already run `map.remove()`, so `addTo` operates on a torn-down map — Leaflet throws `TypeError: Cannot read properties of undefined (reading appendChild)` from `NewClass.onAdd`, surfaced via the catch as `console.error("Failed to load GeoJSON: ...")`. Cosmetic country-borders basemap only — zero data dependency; the revenue bubbles and totals render correctly. Discovered while adding e2e/map-revenue-by-region-scale.spec.ts for the _resolve_map_data fix; the spec filters this specific error so it does not mask the data assertions. Fix: guard the `.then` with an `isMounted` ref / `AbortController`, or skip `addTo` when `mapRef.current` no longer matches the captured `map`. Pre-existing — MapWidget Leaflet integration, not touched by the resolver fix. | severity: cosmetic | blocking: console-error noise on every dashboard carrying a map widget; no functional impact

23. 2026-05-31 | finops-domain-decision-d | src/nlq/config/metric_concept_map.yaml:700 (cloud_spend) + src/nlq/services/dcl_semantic_client_v2.py:get_metric + dcl backend/api/query.py:execute_query | cloud_spend resolves to a metric but never to a VALUE — the CTO/CFO dashboard tiles and the Ask answer for cloud_spend render "No data for 'cloud_spend'" even when ~4,200 cloud_spend triples are ingested (verified: Farm POST /api/cloud-spend/run for entity CloudFleet-DEMO landed 4,217 triples; NLQ Ask "cloud spend" → resolved_metric=cloud_spend, intent=POINT_QUERY, value=None). Root cause is an ontology-shape mismatch, NOT the routing (routing landed this session): metric_concept_map maps cloud_spend → concept "cloud_spend.summary" / property "total_cost", but the Farm cloud_spend generator emits per-resource/per-period triples (concept "cloud_spend.cloud_spend_period.bucket-<id>" / property "cost_usd", period monthly e.g. "2026-04") with NO pre-aggregated summary triple. Resolving the monthly total requires period-aware aggregation that sums per-resource cost_usd WITHIN a billing period (not across periods, which would overcount) — _try_total_synthesis only triggers on a ".total" concept and sums across all browsed triples regardless of period, so it is unsafe here. The dashboard path (DCL /api/dcl/query → execute_query) additionally returns no cloud_spend data because DCL's metric materializer does not aggregate the cloud_spend ontology. This is the AAM Blueprint v3.1 "linchpin" (route cloud-spend through AAM into DCL + DCL-side materialization, §3.6/§8 #9 "Partial", NOT STARTED) — out of scope for "delete finops persona + route cloud_spend → CTO/CFO". Fix needs: (a) DCL-side cloud_spend metric materialization (period-aware sum of cost_usd across resources) — DCL-owned; (b) NLQ metric_concept_map alignment to the materialized concept/property once DCL defines it. See aam_deferred_work.md (linchpin). | severity: degraded | blocking: cloud_spend tile/Ask shows "No data" despite ingested triples; cloud_spend is routed/visible to CTO/CFO but carries no value until the linchpin lands

24. 2026-05-31 | finops-domain-decision-d | ENVIRONMENT (DCL prod DB gdbmdrouocxjxiohpixr + shared Redis Cloud) | TWO environment findings surfaced during live verification of this session's work. (A) **Local stack runs against PROD DCL.** The pm2 services + aos-launch.sh source `.env`, whose DCL credentials point at the prod DCL Supabase project (gdbmdrouocxjxiohpixr) and a shared remote Redis Cloud (rediss://...redislabs.com:16016) — NOT aos-dev. `.env` also had a stale `DCL_API_URL=8104` (dead) while DCL listens on 8004; nlq-backend was repointed to `.env.development` (8004) this session to make NLQ healthy for verification. Net: local dev/test queries + ingests hit prod DCL data — the dev/prod separation in the root CLAUDE.md is not actually enforced by the launcher. Decision needed: fix the launcher/.env to use aos-dev for local, OR document that "local" intentionally reads prod DCL. (B) **CloudFleet-DEMO synthetic ingest left a Redis-buffer remnant in prod DCL.** To verify cloud_spend live, Farm `POST /api/cloud-spend/run` ingested entity `CloudFleet-DEMO` (run_id 230cd96d-badf-43d9-82a6-922b9facd870, 4,217 synthetic cloud_spend triples) into prod DCL. The DB rows were deleted this session (`DELETE FROM public.semantic_triples WHERE entity_id='CloudFleet-DEMO'` → 4,217 removed; /api/dcl/entities + /api/dcl/snapshots confirm gone). BUT DCL's ingest buffer (shared Redis + the running process's in-memory copy) still holds it, so NLQ `/api/v1/entities` (registry → buffer-backed catalog) still lists CloudFleet-DEMO as entities[0]. Full eviction needs: delete Redis keys `dcl:ingest:rows:230cd96d-...:<pipe_id>`, the `dcl:ingest:receipts` hash field, and `dcl:ingest:materialized:*` for this run, THEN restart dcl-backend (rehydrates in-memory from cleaned Redis). NOT done — Redis surgery + restart on shared prod infra is a consequential action requiring authorization (consequences rule); the remnant is a low-harm phantom synthetic entity (DB authoritative store is clean) but it is picked by fragile `entities[0]` test fixtures. This remnant is independent of the pre-existing data-poor-entity test failures (#1/#11/#18/#20): those fixtures select entities[0] without checking it carries the queried metric, so they fail on test-finops-wp4 etc. regardless. | severity: degraded | blocking: prod DCL buffer carries a phantom CloudFleet-DEMO entity until a scoped Redis-clean+restart; local stack silently operates on prod DCL data

25. 2026-06-04 | v4-nlq-display-and-pre-existing | src/nlq/knowledge/schema.py (revenue unit) + src/nlq/api/routes.py (count fmt, DONE) + tests/test_cross_module_contract.py + tests/test_dcl_adapter_integration.py | Found completing the v4 four-fabric HUMAN read-back (DCL aggregators added this session, aam_deferred_work.md #52 / dcl_deferred_work.md #52 — NLQ now answers all four domains). Four NLQ-side items. (1) REVENUE $M MIS-SCALE: NLQ renders transaction revenue under its financial-statement convention — get_metric_unit("revenue")="USD millions" (FINANCIAL_SCHEMA in schema.py) → routes.py formats "$134625.0M". The four-fabric revenue is order-level ACTUAL dollars (revenue.total=134625.0, verified == Σ live Confluent order amount_usd), so "$134625.0M" reads as $134B — wrong magnitude, VALUE CORRECT. Changing revenue's schema unit to "USD" would mis-display REAL financial revenue (stored in $M) — unsafe. Proper fix: scale-aware display — respect the triple's unit (DCL emits unit=usd on revenue.total) when it conflicts with the metric's reporting unit, or a distinct transaction-revenue metric. (2) COUNT DISPLAY — FIXED this session (routes.py): the count branch now also fires when canonical_unit=="count" (customer_count/support_tickets display unit is customers/tickets, which canonicalize to count), so "6"/"48" not "6.0"/"48.0". (3) MULTI-METRIC PHRASING: some verbose count phrasings ("how many customers do we have", "number of support tickets") route through the multi-metric parser and 422 multi_metric_query_failed, while direct phrasings ("customer count", "how many customers", "support tickets") work — NLQ question-parser sensitivity, pre-existing. (4) PRE-EXISTING TEST FAILURES (verified fail on pristine committed 9c89523, NOT caused by #52 or the count fix — confirmed by stashing all working-tree changes): test_cross_module_contract.py::TestErrorPathContracts::test_no_dcl_url_raises + test_dcl_adapter_integration.py::TestEndToEndDCLQuery::test_full_pipeline_no_dcl_raises. Both construct DCLSemanticClient(dcl_base_url=None) expecting RuntimeError "DCL_API_URL not configured", but the dev env sets DCL_API_URL and the tests do not monkeypatch.delenv it, so the v1 client uses the env URL and never raises. Environmental test-infra gap (add monkeypatch.delenv("DCL_API_URL")), NOT an A1 silent-fallback bug (the client correctly uses the configured URL). | severity: degraded | blocking: (1) a clean revenue display in the demo (value is correct); (4) the NLQ contract/adapter suite shows 2/93 red in any DCL-configured env
    RESOLVED 2026-05-31 (Ilya directive) — (A) env files corrected: `.env`=prod (DCL 8004), `.env.development`=dev (DCL 8104, glmeqbn) — they now match their names. dev DCL came up on 8104; nlq-backend repointed to `.env.development` (8104) and restarted, so NLQ is OFF prod and on dev (verified: NLQ /api/v1/entities = 66 dev entities, CloudFleet-DEMO absent). (B) CloudFleet-DEMO fully purged from prod DCL with NO Redis deletion required: the `/api/dcl/ingest-triples` path the Farm cloud-spend run used writes only to `semantic_triples` (DB) — it has zero Redis footprint (the `dcl:ingest:*` rows/receipts/materialized buffer is the legacy AAM-runner path, which never held CloudFleet). The DB delete (4,217 rows) + prod DCL catalog refresh purged it everywhere — verified clean on prod :8004 `/api/dcl/entities`, `/api/dcl/semantic-export`, and `triples/browse`; prod Redis scans for `*CloudFleet*`, `*230cd96d*` (run_id), and `dcl:*69688df3*` (tenant) all returned 0. The prod dcl-backend restart was NOT needed and was NOT performed (gate respected). Finding (A) — that the launcher sources `.env` and silently ran local work against prod DCL — remains a standing environment risk to fix in the launcher; logged here and in memory.
